apiVersion: v1
kind: Service
metadata:
  name: {{ include "dm.fullname" . }}
  labels:
    {{- include "dm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dm.selectorLabels" . | nindent 4 }}
